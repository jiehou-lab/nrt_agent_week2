---
description: Measure and classify one slide, with the uncertainty carried through
argument-hint: <slide path, e.g. data/case_003.pgm>
---

Analyse **$1**.

Slides available:
!`ls data/*.pgm`

Delegate to the `tissue-analyst` subagent. When it returns, give me the numbers,
the label, and — in one sentence — whether I should record that label.

Do not open any `.pgm` file yourself.
